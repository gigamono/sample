meta:
  kind: api
  version: 0.0.1
  runtime_version: 0.0.1

permissions:
  http_event:
    read_request: ["api/v1/body/**"]
    send_request: ["api/v1/static/**"]

middlewares:
  - script: mine/middleware.js

authentication:
  enabled: true
