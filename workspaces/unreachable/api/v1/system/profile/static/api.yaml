meta:
  kind: api
  version: 0.0.1
  runtime_version: 0.0.1

permissions:
  fs:
    open: ["/data/api/v1/system/profile/**"]
    read: ["/data/api/v1/system/profile/**"]

  http_event:
    read_request: true
    send_response: true

authentication:
  enabled: false
