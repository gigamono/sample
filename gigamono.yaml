meta:
  kind: gigamono
  version: "1"

engines:
  runtime:
    root_path: ../sample
    db_url: mysql://appcypher@localhost/db
    js_runtime:
      enable_snapshot: false

web_ui:
  dir: ../web_ui/build
