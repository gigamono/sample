meta:
  kind: gigamono
  version: "1"

js_runtime:
  enable_snapshot: false

volume:
  root: ../sample
  multi_workspace: true

db:
  user: db
  host: localhost
  port: 3306
  multi_workspace: true

ui:
  root: ../web_ui/build
